import MenuAdd from "@/components/menu/MenuAdd";
import MenuHeader from "@/components/menu/MenuHeader";

const Page = () => {
  return (
    <div>
      <MenuHeader />
      <MenuAdd />
    </div>
  );
};

export default Page;
